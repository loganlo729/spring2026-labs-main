## Problem 1: Unique Values (Order-Preserving)

Write:
```python
def uniques_in_order(nums: list[int]) -> list[int]:
    ...
```

Return values that appear **exactly once**, preserving original order.
Do not use an O(n^2) nested-loop approach.
